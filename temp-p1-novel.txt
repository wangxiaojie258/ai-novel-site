commit 1fb9bac17ac3761a8a2f79e736b2c7b93f3cffa6
Author: wangxiaojie258 <[email protected]>
Date:   Thu Jul 9 00:03:10 2026 +0800

    P1: about page + SEO + model attribution

diff --git a/src/pages/novels/[id]/index.astro b/src/pages/novels/[id]/index.astro
index 491140e..8af55a1 100644
--- a/src/pages/novels/[id]/index.astro
+++ b/src/pages/novels/[id]/index.astro
@@ -35,9 +35,34 @@ const chapterItems = novelChapters.map((c) => ({
 }));
 
 const firstChapterHref = chapterItems.length > 0 ? chapterItems[0].href : '#';
+
+const bookUrl = `https://aibooks.cc.cd/novels/${novelSlug}`;
+const structuredData = {
+  '@context': 'https://schema.org',
+  '@type': 'Book',
+  name: novel.title,
+  author: {
+    '@type': 'Person',
+    name: novel.author,
+    description: `AI 模型: ${novel.aiModel ?? '未知'}`,
+  },
+  description: novel.excerpt ?? '',
+  numberOfPages: novel.chapterCount,
+  inLanguage: 'zh-CN',
+  genre: novel.tags ?? [],
+  url: bookUrl,
+  image: novel.cover ? `https://aibooks.cc.cd${novel.cover}` : undefined,
+  ...(novel.aiModel ? { publisher: { '@type': 'Organization', name: `AI 模型: ${novel.aiModel}` } } : {}),
+};
 ---
 
-<BaseLayout title={novel.title} description={novel.excerpt ?? novel.title}>
+<BaseLayout
+  title={novel.title}
+  description={novel.excerpt ?? novel.title}
+  ogType="book"
+  ogImage={novel.cover ?? undefined}
+  structuredData={structuredData}
+>
   <article class="page">
     <header class="novel-header">
       {novel.cover && (
